const sum = (num1, num2) => {
  return num1 + num2;
};

console.log(sum(10, 5));

// const multiply = (num1, num2) => {
//   return num1 * num2;
// };

function multiply(num1, num2) {
  return num1 * num2;
}

console.log(multiply(10, 5));
